# prometheus/prometheus.yml
global:
  scrape_interval: 15s
  evaluation_interval: 30s

rule_files:
  - /etc/prometheus/rules/llm.yml

scrape_configs:
  - job_name: 'otel-collector-llm'
    static_configs:
      - targets: ['otel-collector-llm:8890']
    metrics_path: /metrics